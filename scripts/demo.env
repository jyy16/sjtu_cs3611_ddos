# Copy this file to scripts/demo.env, then adjust values for your lab.
# Keep TARGET_IP in loopback/private ranges only. Never use a public target.

PYTHON=.venv/bin/python
SUDO=sudo

# Local demo defaults. For Mininet, set START_TARGET=0 and use the victim host IP.
TARGET_IP=127.0.0.1
TARGET_PORT=8080
TARGET_URL=http://127.0.0.1:8080/
# WSL localhost traffic may not be visible on lo; any works with the SLL/SLL2 parser.
CAPTURE_IFACE=any
START_TARGET=1
TARGET_SITE_DIR=demo_site

# Demo duration and traffic rates. Keep rates modest for class demos.
NORMAL_SECONDS=10
ATTACK_SECONDS=20
NORMAL_RATE=5
SYN_RATE=200
HTTP_RATE=80
UDP_RATE=100

# Realtime demo settings for scripts/run_realtime_demo.sh.
# The attack runs in the foreground while the background defense loop repeats:
# capture window -> extract features -> infer -> apply automatic blocking.
REALTIME_ATTACK_SECONDS=120
REALTIME_WINDOW_SECONDS=4
REALTIME_GRACE_SECONDS=2
DEFENSE_TTL=300
# Keep 0 for a cleaner realtime AI-driven blocking demo.
# Set to 1 if you also want baseline rate-limit rules active before the attack starts.
INSTALL_BASELINE=0

# Basic defense thresholds.
SYN_LIMIT=50
HTTP_LIMIT=120
CONFIDENCE_THRESHOLD=0.80
DEFENSE_BACKEND=iptables

# Structured storage for final grading.
# Keep "none" for quick local runs. Start Redis and set "redis" for the final demo.
STORAGE_BACKEND=none
REDIS_URL=redis://127.0.0.1:6379/0
STORAGE_KEY_PREFIX=cs3611:ddos
# STORAGE_FAIL_OPEN=1 allows the demo to continue if Redis is unavailable.

# Optional nftables backend:
# DEFENSE_BACKEND=nftables
#
# Optional nginx edge demo:
# Start the origin on 127.0.0.1:8080, run nginx with defense/nginx.conf,
# then send HTTP traffic through the edge by overriding TARGET_URL:
# TARGET_URL=http://127.0.0.1:8081/

# Model and training files.
MODEL_PATH=models/saved/model.pth
TRAIN_FEATURES=data/features/train.csv
TRAIN=0

# Output tag and folder suffix.
PROJECT_TAG=cs3611-ddos
